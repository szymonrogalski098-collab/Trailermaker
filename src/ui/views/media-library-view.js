/**
 * Media library panel controller (import + thumbnail grid).
 * Rendering/interaction logic lands in ETAP 2.
 */
export class MediaLibraryView {
  /** @param {HTMLElement} rootElement */
  constructor(rootElement) {
    this.rootElement = rootElement;
  }

  // TODO(ETAP-2): render import controls + media grid bound to MediaManager
}
