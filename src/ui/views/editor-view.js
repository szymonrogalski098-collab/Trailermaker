/**
 * Root editor view controller. Rendering/interaction logic lands in ETAP 2.
 */
export class EditorView {
  /** @param {HTMLElement} rootElement */
  constructor(rootElement) {
    this.rootElement = rootElement;
  }

  // TODO(ETAP-2): render media panel, timeline, preview canvas, settings/template panels
}
