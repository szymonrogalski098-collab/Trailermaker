/**
 * Timeline view controller (clip strip, drag/reorder, effect/text editing).
 * Rendering/interaction logic lands in ETAP 2.
 */
export class TimelineView {
  /** @param {HTMLElement} rootElement */
  constructor(rootElement) {
    this.rootElement = rootElement;
  }

  // TODO(ETAP-2): render clip strip bound to TimelineEngine
}
